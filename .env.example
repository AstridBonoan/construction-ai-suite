# Construction AI Suite - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# FLASK APPLICATION (REQUIRED)
# ============================================================================
# Environment type: production, staging, development
FLASK_ENV=development

# Enable debug mode (should be false in production)
FLASK_DEBUG=false

# Flask secret key for sessions (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# OPTIONAL: Will use a default insecure key in development only
# REQUIRED in production
# FLASK_SECRET_KEY=your-secret-key-here

# ============================================================================
# LOGGING (OPTIONAL - defaults work for most cases)
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Directory for log files (will be created if doesn't exist)
LOG_DIR=logs

# ============================================================================
# DATABASE (OPTIONAL - not required for current demo)
# ============================================================================
# Database URL (if you add database support)
# DATABASE_URL=postgresql://user:password@localhost:5432/construction_ai

# ============================================================================
# MODEL CONFIGURATION (OPTIONAL)
# ============================================================================
# Path to models directory (defaults to ./models/)
# MODELS_DIR=models

# Path to model registry (defaults to ./models/registry.json)
# MODEL_REGISTRY=models/registry.json

# ============================================================================
# DATA CONFIGURATION (OPTIONAL)
# ============================================================================
# Path to data directory (defaults to ./data/)
# DATA_DIR=data

# Path to demo data (defaults to ./scripts/demo_data.json)
# DEMO_DATA=scripts/demo_data.json

# ============================================================================
# DEMO MODE (OPTIONAL)
# ============================================================================
# Enable demo mode: true or false (defaults to false)
# In demo mode, the system uses sample data and provides stable results
DEMO_MODE=false

# ============================================================================
# SECURITY (OPTIONAL - for API integrations)
# ============================================================================
# API Key for external integrations (if needed)
# API_KEY=your-api-key-here

# JWT Secret for token-based auth (if needed)
# JWT_SECRET=your-jwt-secret-here

# ============================================================================
# MONITORING & PERFORMANCE (OPTIONAL)
# ============================================================================
# Enable performance monitoring: true or false (defaults to true in production)
ENABLE_PERFORMANCE_MONITORING=true

# Memory warning threshold (percentage, 0-100)
MEMORY_WARNING_THRESHOLD=80

# ============================================================================
# NOTES
# ============================================================================
# Required for production: FLASK_ENV, FLASK_SECRET_KEY
# Required for demo: DEMO_MODE=true
# Optional: All others have sensible defaults
# 
# Secrets should NEVER be committed to git:
# - Use environment variables for sensitive values
# - Never hardcode API keys, passwords, or secrets
# - Use a secrets manager in production
