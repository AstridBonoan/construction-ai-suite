# Model Card - v8 exploratory baseline

Dataset: data_splits/v8 (project_level_aggregated_v8.csv)
Note: actual_start_imputed rows WERE INCLUDED in training (flag present as a feature if available).

WARNING: Single-class or no positive examples in training set; skipping model training.
