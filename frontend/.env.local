# Backend API Configuration
# Local development (default)
VITE_BACKEND_URL=http://localhost:5000

# For ngrok sharing, replace above with:
# VITE_BACKEND_URL=https://your-backend-ngrok-url.ngrok.io
# Then restart the frontend dev server with: npm run dev
